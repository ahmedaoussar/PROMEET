import { Formulaire } from "../Composants/Formulaire"
import SearchBar from '../component/SearchBar'; // Importez le composant de barre de recherche

export const Accueil = () => {
    return (
        <div>
            <Formulaire/>
           <SearchBar />
        </div>
    )
}

